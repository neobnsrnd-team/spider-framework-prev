<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
    CmsStatistics Mapper
    - SPW_CMS_PAGE_VIEW_LOG : 페이지 조회/클릭 이벤트 로그 (READ only)
    - SPW_CMS_PAGE          : 페이지명 JOIN 용
-->
<mapper namespace="com.example.spideradmin.domain.cmsstatistics.mapper.CmsStatisticsMapper">

    <!-- ==================== 공통 WHERE 조건 ==================== -->

    <sql id="statConditions">
        <where>
            l.VIEW_DTIME &gt;= TO_TIMESTAMP(#{req.startDate}, 'YYYY-MM-DD')
            AND l.VIEW_DTIME <![CDATA[<]]> (TO_TIMESTAMP(#{req.endDate}, 'YYYY-MM-DD') + INTERVAL '1' DAY)
            AND p.USE_YN = 'Y'
            <if test="req.pageId != null and req.pageId != ''">
                AND l.PAGE_ID = #{req.pageId}
            </if>
        </where>
    </sql>

    <!-- ==================== 통계 목록 조회 (페이지네이션) ==================== -->

    <select id="findStatList" resultType="com.example.spideradmin.domain.cmsstatistics.dto.CmsStatisticsResponse">
        SELECT * FROM (
            SELECT INNER_QRY.*, ROWNUM AS RNUM FROM (
                SELECT
                    s.PAGE_ID                                               AS pageId,
                    s.PAGE_NAME                                             AS pageName,
                    s.VIEW_MODE                                             AS viewMode,
                    CASE WHEN MAX(h.FILE_ID) IS NOT NULL
                        THEN 'http://'
                             || MAX(si.INSTANCE_IP) KEEP (
                                    DENSE_RANK LAST ORDER BY h.LAST_MODIFIED_DTIME, h.FILE_ID, h.INSTANCE_ID
                                )
                             || ':'
                             || TRIM(TO_CHAR(MAX(si.INSTANCE_PORT) KEEP (
                                    DENSE_RANK LAST ORDER BY h.LAST_MODIFIED_DTIME, h.FILE_ID, h.INSTANCE_ID
                                )))
                             || '/cms/deployed/' || s.PAGE_ID || '.html'
                    END                                                     AS deployedUrl,
                    s.VIEW_COUNT                                            AS viewCount,
                    s.CLICK_COUNT                                           AS clickCount
                FROM (
                    SELECT
                        p.PAGE_ID                                           AS PAGE_ID,
                        p.PAGE_NAME                                         AS PAGE_NAME,
                        p.VIEW_MODE                                         AS VIEW_MODE,
                        COUNT(CASE WHEN l.EVENT_TYPE = 'VIEW'  THEN 1 END) AS VIEW_COUNT,
                        COUNT(CASE WHEN l.EVENT_TYPE = 'CLICK' THEN 1 END) AS CLICK_COUNT
                    FROM SPW_CMS_PAGE_VIEW_LOG l
                    JOIN SPW_CMS_PAGE p ON l.PAGE_ID = p.PAGE_ID
                    <include refid="statConditions"/>
                    GROUP BY p.PAGE_ID, p.PAGE_NAME, p.VIEW_MODE
                ) s
                LEFT JOIN FWK_CMS_FILE_SEND_HIS h
                    ON h.FILE_ID LIKE s.PAGE_ID || '\_v%.html' ESCAPE '\'
                   AND h.FILE_ID NOT LIKE '%\_expired.html' ESCAPE '\'
                LEFT JOIN FWK_CMS_SERVER_INSTANCE si ON h.INSTANCE_ID = si.INSTANCE_ID
                GROUP BY s.PAGE_ID, s.PAGE_NAME, s.VIEW_MODE, s.VIEW_COUNT, s.CLICK_COUNT
                ORDER BY s.VIEW_COUNT DESC, s.PAGE_NAME ASC
            ) INNER_QRY
            WHERE ROWNUM <![CDATA[<=]]> #{endRow}
        ) WHERE RNUM > #{offset}
    </select>

    <select id="countStatList" resultType="long">
        SELECT COUNT(*) FROM (
            SELECT l.PAGE_ID
            FROM SPW_CMS_PAGE_VIEW_LOG l
            JOIN SPW_CMS_PAGE p ON l.PAGE_ID = p.PAGE_ID
            <include refid="statConditions"/>
            GROUP BY l.PAGE_ID
        )
    </select>

    <!-- ==================== 컴포넌트 클릭 상세 ==================== -->

    <select id="findDetailList"
            resultType="com.example.spideradmin.domain.cmsstatistics.dto.CmsStatisticsDetailResponse">
        SELECT
            l.COMPONENT_ID                                                  AS componentId,
            COUNT(*)                                                        AS clickCount
        FROM SPW_CMS_PAGE_VIEW_LOG l
        WHERE l.PAGE_ID = #{req.pageId}
          AND l.EVENT_TYPE = 'CLICK'
          AND l.COMPONENT_ID IS NOT NULL
          AND l.VIEW_DTIME &gt;= TO_TIMESTAMP(#{req.startDate}, 'YYYY-MM-DD')
          AND l.VIEW_DTIME <![CDATA[<]]> (TO_TIMESTAMP(#{req.endDate}, 'YYYY-MM-DD') + INTERVAL '1' DAY)
        GROUP BY l.COMPONENT_ID
        ORDER BY clickCount DESC, l.COMPONENT_ID ASC
    </select>

</mapper>
